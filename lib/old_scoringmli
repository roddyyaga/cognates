(*
open Base

type encoders_table = (string, string list -> int array) Hashtbl.t

type 'a weights_table =
  (string * string, (int, 'a) Owl.Dense.Ndarray.Generic.t) Hashtbl.t

val possible_pairs :
  Utils.dataset_row list -> (Utils.dataset_row * Utils.dataset_row) list
(** Generate all pairs of dataset rows in a list that might be cognate (have the same gloss id) *)

val align_pairs :
  encoders_table ->
  'a weights_table ->
  (Utils.dataset_row * Utils.dataset_row) list ->
  ( (Utils.dataset_row * Utils.dataset_row)
  * ( (int, Bigarray.int_elt) Owl.Dense.Ndarray.Generic.t
    * (int, Bigarray.int_elt) Owl.Dense.Ndarray.Generic.t
    * int ) )
  list

(** Given a table mapping pairs of languages to weights, produce alignments for a list of pairs of rows *)
  *)
